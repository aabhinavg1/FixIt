---
title: "-fcheck-array-temporaries"
slug: "/tools/clang-flags/-fcheck-array-temporaries/"
description: "Flags that will be passed onto the ``gfortran`` compiler when Clang is given a Fortran input."
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="-fcheck-array-temporaries">

## What this flag actually does

Flags that will be passed onto the ``gfortran`` compiler when Clang is given a Fortran input.



It can be negated with -fno-check-array-temporaries.

## When to use it

- You are targeting Fortran compilation options and want this flag behavior applied to the whole compilation.
- You are compiling with Clang (the option is not accepted by other compilers).

## When not to use it

- Your build targets a different architecture — this flag is accepted only when targeting Fortran compilation options.
- You need the same behavior from GCC or another compiler that does not implement this option.

## Usage example

```bash
clang++ -fcheck-array-temporaries main.cpp -o app
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: ClangOption
- **Stage**: driver, category `Fortran compilation`
- **Negated by**: `-fno-check-array-temporaries`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
