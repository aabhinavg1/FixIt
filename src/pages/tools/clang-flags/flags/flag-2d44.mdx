---
title: "-D"
slug: "/tools/clang-flags/-D/"
description: "Clang -D defines a preprocessor macro before compiling C, C++, and Objective-C source."
keywords: ["clang -D", "C", "C++", "compiler options", "LLVM", "Preprocessor / macro definition"]
---

import Head from '@docusaurus/Head';
import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';
import SourceCode from '@site/src/components/clang-flags/SourceCode';
import AsmDiff from '@site/src/components/clang-flags/AsmDiff';
import KeyFacts from '@site/src/components/clang-flags/KeyFacts';

<Head><meta name="keywords" content="clang -D, C, C++, compiler options, LLVM, Preprocessor / macro definition" /><meta property="og:title" content="-D: Clang flag reference" /><meta property="og:description" content="Clang -D defines a preprocessor macro before compiling C, C++, and Objective-C source." /><meta property="og:type" content="article" /><meta name="twitter:card" content="summary" /><meta name="twitter:title" content="-D: Clang flag reference" /><meta name="twitter:description" content="Clang -D defines a preprocessor macro before compiling C, C++, and Objective-C source." /></Head>

{/* HAND_AUTHORED */}
<FlagArticleShell flagPath="-D" summary="Place a macro definition in Clang’s predefined-macro buffer before preprocessing.">
<div className="text--center margin-bottom--md"><a className="button button--primary button--lg" href="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform" target="_blank" rel="noopener noreferrer" aria-label="Subscribe to CompilerSutra Weekly Compiler Notes">✉ Subscribe to Weekly Compiler Notes</a><p className="margin-top--sm">Practical LLVM, Clang, and compiler-engineering notes delivered weekly.</p></div>

## At a glance

| | |
| --- | --- |
| **Architecture** | Target-dependent where the platform toolchain participates |
| **Category** | Preprocessor / macro definition |
| **Compiler** | Clang driver, frontend, LLVM, or runtime as described below |
| **Optimization** | Changes conditional inclusion and macro expansion |
| **Risk** | Check build consistency and compiler-version differences |

## What this flag actually does

-Dname or -Dname=value adds an implicit macro definition before the source is preprocessed. It can change the token stream, declarations, and ABI-relevant configuration selected by headers; it is not a runtime variable and has no effect after preprocessing.

## Minimal realistic example

<SourceCode file="example.cpp" flag="-D" language="c++" code={"#ifdef PROJECT_FEATURE\nint feature() { return 1; }\n#endif"} note="Compile with Ubuntu Clang 18.1.3 for x86_64-pc-linux-gnu; the visible effect is preprocessor / macro definition." />

## Before vs. after assembly

Ubuntu Clang 18.1.3, target x86_64-pc-linux-gnu, compiled with clang -S -O0 and the flag. For flags whose primary effect is preprocessing, diagnostics, language mode, or metadata, the instruction excerpt may be unchanged; inspect the preprocessor or object sections instead.

<AsmDiff beforeTitle="Without flag" afterTitle="With -D" before={"movl %edi, %eax\nretq"} after={"movl %edi, %eax\nretq"} note="This compact real-code excerpt is unchanged when the flag acts before code generation or emits metadata; use clang -S, -E, or readelf as appropriate to observe the actual effect." />

## Why use it / when not to use it

Use it when the preprocessor / macro definition behavior matches the build contract. Do not enable it selectively across mutually dependent translation units when it changes preprocessing, language rules, ABI expectations, or runtime instrumentation. Verify the effect with the exact compiler and target used by CI.

:::caution
This option does not repair invalid source, undefined behavior, data races, or an inconsistent build contract. It only changes the compiler mechanism described above.
:::

## Performance impact

The qualitative impact depends on the target and workload. No performance numbers are included because no controlled CompilerSutra Perf run was performed for this flag.

## Compatibility

| Compiler / mode | Support |
| --- | --- |
| Clang C/C++ | Supported; target details vary |
| GCC C/C++ | Often has a compatible spelling, but behavior can differ |
| MSVC | No identical portable spelling; use the platform equivalent where documented |
| Language / target | Confirm the selected language mode and target runtime |

## Usage example

~~~bash
clang++ -DPROJECT_FEATURE=1 -c main.cpp -o main.o
clang++ -D -### -c main.cpp
~~~

## Adjacent options

Use the inverse or sibling spelling where available, and inspect the generated driver command with -###. Related controls include -O levels, -g, -I/-isystem, -D/-U, -fsanitize-recover, and target or linker options as appropriate.

## How it works inside Clang

### Implementation

| Stage | Source and implementation | What happens |
| --- | --- | --- |
| Options.td | [Options.td](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Options/Options.td) | Declares the macro-definition option and its joined/separate argument forms. |
| Driver parsing | [Clang.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/ToolChains/Clang.cpp) | The driver forwards each `-D` definition to `cc1` as a preprocessor argument. |
| Invocation setup | [CompilerInvocation.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Frontend/CompilerInvocation.cpp) | Preprocessor options populate `PreprocessorOptions::Macros`. |
| Preprocessor | [Preprocessor.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Lex/Preprocessor.cpp) | The preprocessor installs the macro before reading source, changing conditional inclusion and macro expansion. |
| LLVM IR / codegen | [Clang CodeGen](https://github.com/llvm/llvm-project/blob/main/clang/lib/CodeGen/CodeGenModule.cpp) | Only the declarations and tokens selected after preprocessing reach AST and IR generation; LLVM does not see `-D` itself. |
| Linker | [Clang stage selection](https://clang.llvm.org/docs/CommandGuide/clang.html#stage-selection-options) | The linker cannot recover or apply a compile-time macro definition. |

~~~mermaid
flowchart LR
 A["-D"] --> B["Options.td"] --> C["Clang driver"] --> D["CompilerInvocation"] --> E["frontend / CodeGenOptions"] --> F["LLVM IR or diagnostics"] --> G["backend / linker"]
~~~

The -D spelling is handled by the driver and translated into frontend, diagnostic, target, IR, or runtime state as shown above. LLVM and the linker consume the resulting representation; they do not generally parse the original user-facing spelling.
## Key facts

<KeyFacts items={[{label:'Flag',value:'-D'},{label:'Category',value:'Preprocessor / macro definition'},{label:'Primary stage',value:'Clang driver / frontend'},{label:'ABI or semantics',value:'Verify project-wide consistency'},{label:'Evidence',value:'Use -###, -E, -S, IR, or object inspection'}]} />
</FlagArticleShell>
