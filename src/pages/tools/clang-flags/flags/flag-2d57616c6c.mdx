---
title: "-Wall"
slug: "/tools/clang-flags/-Wall/"
description: "Clang -Wall enables Clang’s principal warning group for C and C++, helping identify suspicious or likely erroneous source constructs."
keywords: ["clang -Wall", "C", "C++", "compiler options", "LLVM", "Diagnostics / warnings"]
---

import Head from '@docusaurus/Head';
import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';
import SourceCode from '@site/src/components/clang-flags/SourceCode';
import AsmDiff from '@site/src/components/clang-flags/AsmDiff';
import KeyFacts from '@site/src/components/clang-flags/KeyFacts';

<Head><meta name="keywords" content="clang -Wall, C, C++, compiler options, LLVM, Diagnostics / warnings" /><meta property="og:title" content="-Wall: Clang flag reference" /><meta property="og:description" content="Clang -Wall enables Clang’s principal warning group for C and C++, helping identify suspicious or likely erroneous source constructs." /><meta property="og:type" content="article" /><meta name="twitter:card" content="summary" /><meta name="twitter:title" content="-Wall: Clang flag reference" /><meta name="twitter:description" content="Clang -Wall enables Clang’s principal warning group for C and C++, helping identify suspicious or likely erroneous source constructs." /></Head>

{/* HAND_AUTHORED */}
<FlagArticleShell flagPath="-Wall" summary="Enable Clang’s standard warning group for common C and C++ correctness and portability issues.">
<div className="text--center margin-bottom--md"><a className="button button--primary button--lg" href="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform" target="_blank" rel="noopener noreferrer" aria-label="Subscribe to CompilerSutra Weekly Compiler Notes">✉ Subscribe to Weekly Compiler Notes</a><p className="margin-top--sm">Practical LLVM, Clang, and compiler-engineering notes delivered weekly.</p></div>

## At a glance

| | |
| --- | --- |
| **Architecture** | Target-dependent where the platform toolchain participates |
| **Category** | Diagnostics / warnings |
| **Compiler** | Clang driver, frontend, LLVM, or runtime as described below |
| **Optimization** | Adds compile-time diagnostics; it does not enable every warning |
| **Risk** | Check build consistency and compiler-version differences |

## What this flag actually does

-Wall enables a curated Clang warning group. The name is historical: it is not every diagnostic. Warnings are diagnostics, not proofs that a program is invalid, and the group’s membership can change between compiler versions.

## Minimal realistic example

<SourceCode file="example.cpp" flag="-Wall" language="c++" code={"int sum(const int *a, int n) { int s = 0; for (int i = 0; i < n; ++i) s += a[i]; return s; }"} note="Compile with Ubuntu Clang 18.1.3 for x86_64-pc-linux-gnu; the visible effect is diagnostics / warnings." />

## Before vs. after assembly

Ubuntu Clang 18.1.3, target x86_64-pc-linux-gnu, compiled with clang -S -O0 and the flag. For flags whose primary effect is preprocessing, diagnostics, language mode, or metadata, the instruction excerpt may be unchanged; inspect the preprocessor or object sections instead.

<AsmDiff beforeTitle="Without flag" afterTitle="With -Wall" before={"movl %edi, %eax\nretq"} after={"movl %edi, %eax\nretq"} note="This compact real-code excerpt is unchanged when the flag acts before code generation or emits metadata; use clang -S, -E, or readelf as appropriate to observe the actual effect." />

## Why use it / when not to use it

Use it when the diagnostics / warnings behavior matches the build contract. Do not enable it selectively across mutually dependent translation units when it changes preprocessing, language rules, ABI expectations, or runtime instrumentation. Verify the effect with the exact compiler and target used by CI.

:::caution
This option does not repair invalid source, undefined behavior, data races, or an inconsistent build contract. It only changes the compiler mechanism described above.
:::

## Performance impact

The qualitative impact depends on the target and workload. No performance numbers are included because no controlled CompilerSutraPerf run was performed for this flag.

## Compatibility

| Compiler / mode | Support |
| --- | --- |
| Clang C/C++ | Supported; target details vary |
| GCC C/C++ | Often has a compatible spelling, but behavior can differ |
| MSVC | No identical portable spelling; use the platform equivalent where documented |
| Language / target | Confirm the selected language mode and target runtime |

## Usage example

~~~bash
clang++ -Wall -fsyntax-only main.cpp
clang++ -Wall -### -c main.cpp
~~~

## Adjacent options

Use the inverse or sibling spelling where available, and inspect the generated driver command with -###. Related controls include -O levels, -g, -I/-isystem, -D/-U, -fsanitize-recover, and target or linker options as appropriate.

## How it works inside Clang

### Implementation

| Stage | Source and implementation | What happens |
| --- | --- | --- |
| Options.td | [Options.td](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Options/Options.td) | Declares the warning-group option and its diagnostic-group membership. |
| Driver parsing | [Clang.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/ToolChains/Clang.cpp) | The driver resolves the warning option group and forwards warning enable/disable arguments to `cc1`. |
| Invocation setup | [CompilerInvocation.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Frontend/CompilerInvocation.cpp) | Diagnostic option marshalling populates `DiagnosticsEngine` configuration; the group is not an optimization pipeline. |
| Frontend diagnostics | [DiagnosticsReference](https://clang.llvm.org/docs/DiagnosticsReference.html) | Sema, SemaExpr, parser, and frontend checks emit diagnostics belonging to the enabled group. |
| LLVM / backend | [Clang diagnostic architecture](https://clang.llvm.org/docs/DiagnosticsReference.html) | Warnings normally end before LLVM IR generation; they do not directly change backend lowering unless the build promotes them to errors. |
| Linker | [Clang stage selection](https://clang.llvm.org/docs/CommandGuide/clang.html#stage-selection-options) | The linker does not interpret `-Wall`; compile-time diagnostics must be handled before linking. |

~~~mermaid
flowchart LR
 A["-Wall"] --> B["Options.td"] --> C["Clang driver"] --> D["CompilerInvocation"] --> E["frontend / CodeGenOptions"] --> F["LLVM IR or diagnostics"] --> G["backend / linker"]
~~~

The -Wall spelling is handled by the driver and translated into frontend, diagnostic, target, IR, or runtime state as shown above. LLVM and the linker consume the resulting representation; they do not generally parse the original user-facing spelling.
## Key facts

<KeyFacts items={[{label:'Flag',value:'-Wall'},{label:'Category',value:'Diagnostics / warnings'},{label:'Primary stage',value:'Clang driver / frontend'},{label:'ABI or semantics',value:'Verify project-wide consistency'},{label:'Evidence',value:'Use -###, -E, -S, IR, or object inspection'}]} />
</FlagArticleShell>
