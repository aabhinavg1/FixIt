---
title: "-std="
slug: "/tools/clang-flags/-std%3D/"
description: "Clang -std= selects the C, C++, Objective-C, or GNU language dialect used to parse and semantically analyze source."
keywords: ["clang -std=", "C", "C++", "compiler options", "LLVM", "Language mode"]
---

import Head from '@docusaurus/Head';
import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';
import SourceCode from '@site/src/components/clang-flags/SourceCode';
import AsmDiff from '@site/src/components/clang-flags/AsmDiff';
import KeyFacts from '@site/src/components/clang-flags/KeyFacts';

<Head><meta name="keywords" content="clang -std=, C, C++, compiler options, LLVM, Language mode" /><meta property="og:title" content="-std=: Clang flag reference" /><meta property="og:description" content="Clang -std= selects the C, C++, Objective-C, or GNU language dialect used to parse and semantically analyze source." /><meta property="og:type" content="article" /><meta name="twitter:card" content="summary" /><meta name="twitter:title" content="-std=: Clang flag reference" /><meta name="twitter:description" content="Clang -std= selects the C, C++, Objective-C, or GNU language dialect used to parse and semantically analyze source." /></Head>

{/* HAND_AUTHORED */}
<FlagArticleShell flagPath="-std=" summary="Choose the language standard and extension policy Clang uses for a translation unit.">
<div className="text--center margin-bottom--md"><a className="button button--primary button--lg" href="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform" target="_blank" rel="noopener noreferrer" aria-label="Subscribe to CompilerSutra Weekly Compiler Notes">✉ Subscribe to Weekly Compiler Notes</a><p className="margin-top--sm">Practical LLVM, Clang, and compiler-engineering notes delivered weekly.</p></div>

## At a glance

| | |
| --- | --- |
| **Architecture** | Target-dependent where the platform toolchain participates |
| **Category** | Language mode |
| **Compiler** | Clang driver, frontend, LLVM, or runtime as described below |
| **Optimization** | Changes accepted syntax, library feature macros, and language semantics |
| **Risk** | Check build consistency and compiler-version differences |

## What this flag actually does

-std= selects a language mode such as c17, c23, c++17, c++20, gnu17, or gnu++20. The frontend uses that mode for parsing, feature availability, predefined macros, and semantic rules; it is not a linker option and must be consistent across compatible translation units.

## Minimal realistic example

<SourceCode file="example.cpp" flag="-std=" language="c++" code={"constexpr int value = 42;\nint main() { return value; }"} note="Compile with Ubuntu Clang 18.1.3 for x86_64-pc-linux-gnu; the visible effect is language mode." />

## Before vs. after assembly

Ubuntu Clang 18.1.3, target x86_64-pc-linux-gnu, compiled with clang -S -O0 and the flag. For flags whose primary effect is preprocessing, diagnostics, language mode, or metadata, the instruction excerpt may be unchanged; inspect the preprocessor or object sections instead.

<AsmDiff beforeTitle="Without flag" afterTitle="With -std=" before={"movl %edi, %eax\nretq"} after={"movl %edi, %eax\nretq"} note="This compact real-code excerpt is unchanged when the flag acts before code generation or emits metadata; use clang -S, -E, or readelf as appropriate to observe the actual effect." />

## Why use it / when not to use it

Use it when the language mode behavior matches the build contract. Do not enable it selectively across mutually dependent translation units when it changes preprocessing, language rules, ABI expectations, or runtime instrumentation. Verify the effect with the exact compiler and target used by CI.

:::caution
This option does not repair invalid source, undefined behavior, data races, or an inconsistent build contract. It only changes the compiler mechanism described above.
:::

## Performance impact

The qualitative impact depends on the target and workload. No performance numbers are included because no controlled CompilerSutraPerf run was performed for this flag.

## Compatibility

| Compiler / mode | Support |
| --- | --- |
| Clang C/C++ | Supported; target details vary |
| GCC C/C++ | Often has a compatible spelling, but behavior can differ |
| MSVC | No identical portable spelling; use the platform equivalent where documented |
| Language / target | Confirm the selected language mode and target runtime |

## Usage example

~~~bash
clang++ -std=c++20 -Wall -c main.cpp -o main.o
clang++ -std= -### -c main.cpp
~~~

## Adjacent options

Use the inverse or sibling spelling where available, and inspect the generated driver command with -###. Related controls include -O levels, -g, -I/-isystem, -D/-U, -fsanitize-recover, and target or linker options as appropriate.

## How it works inside Clang

### Implementation

| Stage | Source and implementation | What happens |
| --- | --- | --- |
| Options.td | [Options.td](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Options/Options.td) | Declares the joined language-standard option and the accepted language-mode spellings. |
| Driver parsing | [Clang.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/ToolChains/Clang.cpp) | The driver validates the language option and forwards the effective mode to `cc1`; it also selects language-specific defaults. |
| Invocation setup | [CompilerInvocation.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Frontend/CompilerInvocation.cpp) | Language option parsing sets `LangOptions`, including standard version, GNU extensions, and feature toggles. |
| Frontend / AST | [LangOptions](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Basic/LangOptions.def) | Parser, Sema, and AST construction consult `LangOptions` to accept syntax and apply language semantics. |
| LLVM IR / codegen | [CodeGenModule](https://github.com/llvm/llvm-project/blob/main/clang/lib/CodeGen/CodeGenModule.cpp) | The selected language mode affects declarations, builtins, ABI-relevant attributes, and generated IR; LLVM does not parse `-std=`. |
| Linker | [Clang stage selection](https://clang.llvm.org/docs/CommandGuide/clang.html#stage-selection-options) | The linker sees objects and libraries, not the source language standard. |

~~~mermaid
flowchart LR
 A["-std="] --> B["Options.td"] --> C["Clang driver"] --> D["CompilerInvocation"] --> E["frontend / CodeGenOptions"] --> F["LLVM IR or diagnostics"] --> G["backend / linker"]
~~~

The -std= spelling is handled by the driver and translated into frontend, diagnostic, target, IR, or runtime state as shown above. LLVM and the linker consume the resulting representation; they do not generally parse the original user-facing spelling.
## Key facts

<KeyFacts items={[{label:'Flag',value:'-std='},{label:'Category',value:'Language mode'},{label:'Primary stage',value:'Clang driver / frontend'},{label:'ABI or semantics',value:'Verify project-wide consistency'},{label:'Evidence',value:'Use -###, -E, -S, IR, or object inspection'}]} />
</FlagArticleShell>
