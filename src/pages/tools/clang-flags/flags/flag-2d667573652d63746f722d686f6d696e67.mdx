---
title: "-fuse-ctor-homing"
slug: "/tools/clang-flags/-fuse-ctor-homing/"
description: "Use constructor homing if we are using limited debug info already"
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="-fuse-ctor-homing">

## What this flag actually does

Use constructor homing if we are using limited debug info already



This is a `-cc1` (internal frontend) option. The Clang driver does not accept it directly, so it must be forwarded with `-Xclang`.



It can be negated with -fno-use-ctor-homing.

## When to use it

- Debug sessions
- Crash investigation
- When you need symbols or richer debug info

## When not to use it

- Release builds without a debugging need
- When size or startup time matters more than observability

## Usage example

```bash
clang++ -Xclang -fuse-ctor-homing -fsyntax-only main.cpp
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: CC1Option, CC1AsOption
- **Stage**: cc1 (frontend), category `Debugging`
- **Negated by**: `-fno-use-ctor-homing`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
