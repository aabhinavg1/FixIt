---
title: "--offload-compression-level="
slug: "/tools/clang-flags/--offload-compression-level%3D/"
description: "Compression level for offload device binaries (HIP only)"
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="--offload-compression-level%3D">

## What this flag actually does

Compression level for offload device binaries (HIP only)



This option expects an argument.

## When to use it

- You are targeting Common Offloading options and want this joined behavior applied to the whole compilation.
- You are compiling with Clang (the option is not accepted by other compilers).

## When not to use it

- Your build targets a different architecture — this joined is accepted only when targeting Common Offloading options.
- You need the same behavior from GCC or another compiler that does not implement this option.

## Usage example

```bash
clang++ --offload-compression-level= main.cpp -o app
```

## Key facts

- **Argument form**: Joined (takes an argument)
- **Accepted by**: ClangOption
- **Stage**: driver, category `Common Offloading`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
