---
title: "-fsanitize=address"
slug: "/tools/clang-flags/-fsanitize%3Daddress/"
description: "Clang -fsanitize=address instruments C and C++ code for AddressSanitizer’s detection of common memory-safety errors."
keywords: ["clang -fsanitize=address", "C", "C++", "compiler options", "LLVM", "Sanitizer / instrumentation"]
---

import Head from '@docusaurus/Head';
import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';
import SourceCode from '@site/src/components/clang-flags/SourceCode';
import AsmDiff from '@site/src/components/clang-flags/AsmDiff';
import KeyFacts from '@site/src/components/clang-flags/KeyFacts';

<Head><meta name="keywords" content="clang -fsanitize=address, C, C++, compiler options, LLVM, Sanitizer / instrumentation" /><meta property="og:title" content="-fsanitize=address: Clang flag reference" /><meta property="og:description" content="Clang -fsanitize=address instruments C and C++ code for AddressSanitizer’s detection of common memory-safety errors." /><meta property="og:type" content="article" /><meta name="twitter:card" content="summary" /><meta name="twitter:title" content="-fsanitize=address: Clang flag reference" /><meta name="twitter:description" content="Clang -fsanitize=address instruments C and C++ code for AddressSanitizer’s detection of common memory-safety errors." /></Head>

{/* HAND_AUTHORED */}
<FlagArticleShell flagPath="-fsanitize=address" summary="Instrument a program with AddressSanitizer checks for out-of-bounds, use-after-free, and related memory errors.">
<div className="text--center margin-bottom--md"><a className="button button--primary button--lg" href="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform" target="_blank" rel="noopener noreferrer" aria-label="Subscribe to CompilerSutra Weekly Compiler Notes">✉ Subscribe to Weekly Compiler Notes</a><p className="margin-top--sm">Practical LLVM, Clang, and compiler-engineering notes delivered weekly.</p></div>

## At a glance

| | |
| --- | --- |
| **Architecture** | Target-dependent where the platform toolchain participates |
| **Category** | Sanitizer / instrumentation |
| **Compiler** | Clang driver, frontend, LLVM, or runtime as described below |
| **Optimization** | Adds runtime checks and sanitizer runtime linkage |
| **Risk** | Check build consistency and compiler-version differences |

## What this flag actually does

-fsanitize=address enables AddressSanitizer instrumentation in Clang’s frontend/codegen pipeline and links or requires the ASan runtime. It can detect many spatial and temporal memory errors at runtime, but it is not a proof of safety and does not diagnose every undefined behavior or data race.

## Minimal realistic example

<SourceCode file="example.cpp" flag="-fsanitize=address" language="c++" code={String.raw`#include <iostream>

int read_value(int *values) {
  return values[1];
}

int main() {
  int values[1] = {7};
  std::cout << read_value(values) << '\n';
}`} note="This complete program reads past a one-element array. Without ASan the behavior is undefined; with ASan, the invalid read is detected at runtime." />

## Before vs. after assembly

Ubuntu Clang 18.1.3, target x86_64-pc-linux-gnu, compiled with -O1 -S. The read_value function reads values[1]; ASan adds a shadow-memory check before the load.

<AsmDiff
  beforeTitle="Without ASan"
  afterTitle="With -fsanitize=address"
  before={String.raw`movl 4(%rdi), %eax
retq`}
  after={String.raw`addq $4, %rdi
movq %rdi, %rax
shrq $3, %rax
movzbl 2147450880(%rax), %eax
testb %al, %al
jne .Lreport
movl (%rdi), %eax
retq
.Lreport:
callq __asan_report_load4@PLT`}
  note="Real excerpts from clang 18.1.3, -O1 -S -masm=att. ASan checks the shadow byte for the address before performing the four-byte load; the runtime report is supplied by compiler-rt."
/>
## Why use it / when not to use it

Use it when the sanitizer / instrumentation behavior matches the build contract. Do not enable it selectively across mutually dependent translation units when it changes preprocessing, language rules, ABI expectations, or runtime instrumentation. Verify the effect with the exact compiler and target used by CI.

:::caution
This option does not repair invalid source, undefined behavior, data races, or an inconsistent build contract. It only changes the compiler mechanism described above.
:::

## Performance impact

AddressSanitizer adds shadow-memory checks, redzones, runtime initialization, and diagnostic reporting. These increase memory use and can materially slow a workload, so ASan builds are for testing, fuzzing, and debugging rather than normal production performance comparisons.

The overhead depends on allocation rate, memory-access density, optimization level, and workload shape. No PerfReport is included because no controlled CompilerSutraPerf run was performed for this page; a valid measurement would need to compare the same workload with and without ASan on the same machine and compiler, while keeping the sanitizer runtime and failure behavior under control.

## Compatibility

| Compiler / mode | Support |
| --- | --- |
| Clang C/C++ | Supported; target details vary |
| GCC C/C++ | Often has a compatible spelling, but behavior can differ |
| MSVC | No identical portable spelling; use the platform equivalent where documented |
| Language / target | Confirm the selected language mode and target runtime |

## Usage example

~~~bash
clang++ -fsanitize=address -fno-omit-frame-pointer -g example.cpp -o app
clang++ -fsanitize=address -### example.cpp -o app
~~~

## Adjacent options

Use the inverse or sibling spelling where available, and inspect the generated driver command with -###. Related controls include -O levels, -g, -I/-isystem, -D/-U, -fsanitize-recover, and target or linker options as appropriate.

## How it works inside Clang

### Implementation

| Stage | Source and implementation | What happens |
| --- | --- | --- |
| Options.td | [Options.td](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Options/Options.td) | Declares the sanitizer option group and sanitizer-kind argument. |
| Driver parsing | [Clang.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/ToolChains/Clang.cpp) | The driver validates sanitizer kinds, forwards instrumentation settings to `cc1`, and arranges the sanitizer runtime at link time. |
| Invocation setup | [CompilerInvocation.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Frontend/CompilerInvocation.cpp) | Sanitizer arguments populate `LangOptions::Sanitize` and `CodeGenOptions` sanitizer state. |
| Code generation | [BackendUtil.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/CodeGen/BackendUtil.cpp) | Clang registers sanitizer callbacks with `PassBuilder`; AddressSanitizer instrumentation and lowering passes are added to the LLVM pipeline. |
| LLVM IR / runtime | [AddressSanitizer pass](https://github.com/llvm/llvm-project/tree/main/llvm/lib/Transforms/Instrumentation/AddressSanitizer) and [compiler-rt](https://github.com/llvm/llvm-project/tree/main/compiler-rt/lib/asan) | LLVM inserts checks and shadow-memory accesses; compiler-rt provides the runtime that reports violations. |
| Linker | [SanitizerRuntime.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/SanitizerArgs.cpp) | The driver selects and links the ASan runtime; the linker itself does not understand the sanitizer semantics. |

~~~mermaid
flowchart LR
 A["-fsanitize=address"] --> B["Options.td"] --> C["Clang driver"] --> D["CompilerInvocation"] --> E["frontend / CodeGenOptions"] --> F["LLVM IR or diagnostics"] --> G["backend / linker"]
~~~

The -fsanitize=address spelling is handled by the driver and translated into frontend, diagnostic, target, IR, or runtime state as shown above. LLVM and the linker consume the resulting representation; they do not generally parse the original user-facing spelling.
## Key facts

<KeyFacts items={[{label:'Flag',value:'-fsanitize=address'},{label:'Category',value:'Sanitizer / instrumentation'},{label:'Primary stage',value:'Clang driver / frontend'},{label:'ABI or semantics',value:'Verify project-wide consistency'},{label:'Evidence',value:'Use -###, -E, -S, IR, or object inspection'}]} />
</FlagArticleShell>
