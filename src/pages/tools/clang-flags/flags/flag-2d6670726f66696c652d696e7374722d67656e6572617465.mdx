---
title: "-fprofile-instr-generate"
slug: "/tools/clang-flags/-fprofile-instr-generate/"
description: "Generate instrumented code to collect execution counts into default.profraw file (overridden by '=' form of option or LLVM_PROFILE_FILE env var)"
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="-fprofile-instr-generate">

## What this flag actually does

Generate instrumented code to collect execution counts into default.profraw file (overridden by '=' form of option or LLVM_PROFILE_FILE env var)



It can be negated with -fno-profile-instr-generate.

## When to use it

- Code generation tuning
- Frontend lowering experiments
- Target-independent compilation control

## When not to use it

- When you are only editing diagnostics or preprocessing
- When the option does not affect your current build phase

## Usage example

```bash
clang++ -fprofile-instr-generate -c main.cpp
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: ClangOption, CLOption
- **Stage**: driver, category `Code Generation`
- **Negated by**: `-fno-profile-instr-generate`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
