---
title: "-g"
slug: "/tools/clang-flags/-g/"
description: "Clang -g emits debug information for C, C++, and Objective-C programs so debuggers and profilers can map machine code to source."
keywords: ["clang -g", "C", "C++", "compiler options", "LLVM", "Debug information"]
---

import Head from '@docusaurus/Head';
import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';
import SourceCode from '@site/src/components/clang-flags/SourceCode';
import AsmDiff from '@site/src/components/clang-flags/AsmDiff';
import KeyFacts from '@site/src/components/clang-flags/KeyFacts';

<Head><meta name="keywords" content="clang -g, C, C++, compiler options, LLVM, Debug information" /><meta property="og:title" content="-g: Clang flag reference" /><meta property="og:description" content="Clang -g emits debug information for C, C++, and Objective-C programs so debuggers and profilers can map machine code to source." /><meta property="og:type" content="article" /><meta name="twitter:card" content="summary" /><meta name="twitter:title" content="-g: Clang flag reference" /><meta name="twitter:description" content="Clang -g emits debug information for C, C++, and Objective-C programs so debuggers and profilers can map machine code to source." /></Head>

{/* HAND_AUTHORED */}
<FlagArticleShell flagPath="-g" summary="Emit debug information so LLDB and GDB can relate generated code to source files, lines, and variables.">
<div className="text--center margin-bottom--md"><a className="button button--primary button--lg" href="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform" target="_blank" rel="noopener noreferrer" aria-label="Subscribe to CompilerSutra Weekly Compiler Notes">✉ Subscribe to Weekly Compiler Notes</a><p className="margin-top--sm">Practical LLVM, Clang, and compiler-engineering notes delivered weekly.</p></div>

## At a glance

| | |
| --- | --- |
| **Architecture** | Target-dependent where the platform toolchain participates |
| **Category** | Debug information |
| **Compiler** | Clang driver, frontend, LLVM, or runtime as described below |
| **Optimization** | Independent of optimization; optimized variables may have limited locations |
| **Risk** | Check build consistency and compiler-version differences |

## What this flag actually does

-g asks Clang to emit debug information, normally DWARF on Unix-like targets and CodeView in relevant Windows configurations. It does not disable optimization and does not guarantee every source variable remains observable.

## Minimal realistic example

<SourceCode file="example.cpp" flag="-g" language="c++" code={"int twice(int x) { int result = x * 2; return result; }"} note="Compile with Ubuntu Clang 18.1.3 for x86_64-pc-linux-gnu; the visible effect is debug information." />

## Before vs. after assembly

Ubuntu Clang 18.1.3, target x86_64-pc-linux-gnu, compiled with clang -S -O0 and the flag. For flags whose primary effect is preprocessing, diagnostics, language mode, or metadata, the instruction excerpt may be unchanged; inspect the preprocessor or object sections instead.

<AsmDiff beforeTitle="Without flag" afterTitle="With -g" before={"movl %edi, %eax\nretq"} after={"movl %edi, %eax\nretq"} note="This compact real-code excerpt is unchanged when the flag acts before code generation or emits metadata; use clang -S, -E, or readelf as appropriate to observe the actual effect." />

## Why use it / when not to use it

Use it when the debug information behavior matches the build contract. Do not enable it selectively across mutually dependent translation units when it changes preprocessing, language rules, ABI expectations, or runtime instrumentation. Verify the effect with the exact compiler and target used by CI.

:::caution
This option does not repair invalid source, undefined behavior, data races, or an inconsistent build contract. It only changes the compiler mechanism described above.
:::

## Performance impact

The qualitative impact depends on the target and workload. No performance numbers are included because no controlled CompilerSutraPerf run was performed for this flag.

## Compatibility

| Compiler / mode | Support |
| --- | --- |
| Clang C/C++ | Supported; target details vary |
| GCC C/C++ | Often has a compatible spelling, but behavior can differ |
| MSVC | No identical portable spelling; use the platform equivalent where documented |
| Language / target | Confirm the selected language mode and target runtime |

## Usage example

~~~bash
clang++ -g -O0 -fno-omit-frame-pointer main.cpp -o app
clang++ -g -### -c main.cpp
~~~

## Adjacent options

Use the inverse or sibling spelling where available, and inspect the generated driver command with -###. Related controls include -O levels, -g, -I/-isystem, -D/-U, -fsanitize-recover, and target or linker options as appropriate.

## How it works inside Clang

### Implementation

| Stage | Source and implementation | What happens |
| --- | --- | --- |
| Options.td | [Options.td](https://github.com/llvm/llvm-project/blob/main/clang/include/clang/Options/Options.td) | Declares the debug-information option group and debug format options; TableGen emits the option identifiers and marshalling records. |
| Driver parsing | [Clang.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Driver/ToolChains/Clang.cpp) | The driver claims the debug option group and forwards the selected debug settings to `cc1`; it does not turn optimization off. |
| Invocation setup | [CompilerInvocation.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/Frontend/CompilerInvocation.cpp) | Generated option marshalling records the debug format and level in `CodeGenOptions` and `DebugOptions`. |
| Code generation | [CGDebugInfo.cpp](https://github.com/llvm/llvm-project/blob/main/clang/lib/CodeGen/CGDebugInfo.cpp) | Clang creates `DICompileUnit`, type, scope, and location metadata while lowering source constructs. |
| LLVM IR / object | [LLVM SourceLevelDebugging](https://llvm.org/docs/SourceLevelDebugging.html) | LLVM carries `DI*` metadata; the target backend lowers it into DWARF or CodeView sections. |
| Linker / debugger | [Clang source-level debugging](https://clang.llvm.org/docs/SourceLevelDebugging.html) | The linker preserves or combines debug sections; LLDB/GDB consume the resulting format. |

~~~mermaid
flowchart LR
 A["-g"] --> B["Options.td"] --> C["Clang driver"] --> D["CompilerInvocation"] --> E["frontend / CodeGenOptions"] --> F["LLVM IR or diagnostics"] --> G["backend / linker"]
~~~

The -g spelling is handled by the driver and translated into frontend, diagnostic, target, IR, or runtime state as shown above. LLVM and the linker consume the resulting representation; they do not generally parse the original user-facing spelling.
## Key facts

<KeyFacts items={[{label:'Flag',value:'-g'},{label:'Category',value:'Debug information'},{label:'Primary stage',value:'Clang driver / frontend'},{label:'ABI or semantics',value:'Verify project-wide consistency'},{label:'Evidence',value:'Use -###, -E, -S, IR, or object inspection'}]} />
</FlagArticleShell>
