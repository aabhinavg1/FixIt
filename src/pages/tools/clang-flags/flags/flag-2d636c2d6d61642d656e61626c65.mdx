---
title: "-cl-mad-enable"
slug: "/tools/clang-flags/-cl-mad-enable/"
description: "OpenCL only. Allow use of less precise MAD computations in the generated binary."
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="-cl-mad-enable">

## What this flag actually does

OpenCL only. Allow use of less precise MAD computations in the generated binary.

## When to use it

- You are targeting OpenCL options and want this flag behavior applied to the whole compilation.

## When not to use it

- Your build targets a different architecture — this flag is accepted only when targeting OpenCL options.

## Usage example

```bash
clang++ -cl-mad-enable main.cpp -o app
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: ClangOption, CC1Option
- **Stage**: cc1 (frontend), category `OpenCL`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
