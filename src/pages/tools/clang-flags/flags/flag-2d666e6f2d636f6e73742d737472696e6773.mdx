---
title: "-fno-const-strings"
slug: "/tools/clang-flags/-fno-const-strings/"
description: "Don't use a const qualified type for string literals in C and ObjC"
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="-fno-const-strings">

## What this flag actually does

Don't use a const qualified type for string literals in C and ObjC



This is a `-cc1` (internal frontend) option. The Clang driver does not accept it directly, so it must be forwarded with `-Xclang`.



It can be negated with -fconst-strings.

## When to use it

- You are driving the frontend directly or forwarding the option with `-Xclang`, since the driver does not expose it.

## When not to use it

- You expect the option to work as a regular driver flag — it requires `-Xclang` forwarding or a direct `clang -cc1` invocation.

## Usage example

```bash
clang++ -Xclang -fno-const-strings -fsyntax-only main.cpp
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: CC1Option
- **Stage**: cc1 (frontend), category `Other`
- **Negated by**: `-fconst-strings`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
