---
title: "--cuda-compile-host-device"
slug: "/tools/clang-flags/--cuda-compile-host-device/"
description: "Compile CUDA code for both host and device (default). Has no effect on non-CUDA compilations."
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="--cuda-compile-host-device">

## What this flag actually does

Compile CUDA code for both host and device (default). Has no effect on non-CUDA compilations.



It is an alias for --offload-host-device.

## When to use it

- You are targeting CUDA options and want this flag behavior applied to the whole compilation.
- You are compiling with Clang (the option is not accepted by other compilers).

## When not to use it

- Your build targets a different architecture — this flag is accepted only when targeting CUDA options.
- You need the same behavior from GCC or another compiler that does not implement this option.

## Usage example

```bash
clang++ --cuda-compile-host-device main.cpp -o app
```

## Key facts

- **Argument form**: Flag (no argument)
- **Accepted by**: ClangOption
- **Stage**: driver, category `CUDA`
- **Alias of**: `--offload-host-device`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
