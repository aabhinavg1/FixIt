---
title: "--driver-mode="
slug: "/tools/clang-flags/--driver-mode%3D/"
description: "Set the driver mode to either 'gcc', 'g++', 'cpp', 'cl' or 'flang'"
---

import FlagArticleShell from '@site/src/components/clang-flags/FlagArticleShell';

{/* GENERATED_BY clang-flags */}
<FlagArticleShell flagPath="--driver-mode%3D">

## What this flag actually does

Set the driver mode to either 'gcc', 'g++', 'cpp', 'cl' or 'flang'



This option expects an argument.

## When to use it

- You are targeting &lt;clang driver internal options> and want this joined behavior applied to the whole compilation.

## When not to use it

- Your build targets a different architecture — this joined is accepted only when targeting &lt;clang driver internal options>.

## Usage example

```bash
clang++ --driver-mode= main.cpp -o app
```

## Key facts

- **Argument form**: Joined (takes an argument)
- **Accepted by**: ClangOption, FlangOption, CLOption, DXCOption
- **Stage**: driver, category `Other`
- **Defined in**: `clang/include/clang/Driver/Options.td`

</FlagArticleShell>
