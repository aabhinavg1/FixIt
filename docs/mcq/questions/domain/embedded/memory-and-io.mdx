---
id: embedded-memory-io
title: "Embedded Memory and I/O MCQs"
slug: /mcq/questions/domain/embedded/memory-and-io
description: "Embedded MCQs on flash, volatile, direct addressing, buses, and memory-mapped device access."
displayed_sidebar: embeddedMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# Embedded Memory and I/O MCQs



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). In an embedded system, which memory is typically used to store the bootloader?" options={['SRAM', 'EEPROM', 'Flash Memory', 'Cache']} answer="Flash Memory" />
<Question question="2). Which addressing mode is most common in embedded assembly programming for accessing I/O devices?" options={['Immediate', 'Direct', 'Indexed', 'Register']} answer="Direct" />
<Question question="3). In embedded C, what is the purpose of the 'volatile' keyword?" options={['Optimize performance', 'Prevent compiler optimization', 'Reduce power usage', 'Increase speed']} answer="Prevent compiler optimization" />
<Question question="4). Which bus is commonly used in automotive embedded systems?" options={['PCIe', 'CAN', 'USB', 'UART']} answer="CAN" />
<Question question="5). In memory-mapped I/O, device registers are accessed using?" options={['Special instructions only', 'Normal memory instructions', 'Dedicated hardware lines', 'Interrupt vectors']} answer="Normal memory instructions" />
<Question question="6). What is the primary role of an embedded bootloader?" options={['Run the application directly', 'Load application code into memory', 'Debug the system', 'Reduce power consumption']} answer="Load application code into memory" />

<div>
    <AdBanner />
</div>
