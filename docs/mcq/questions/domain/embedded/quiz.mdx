---
id: embedded-quiz
title: "Embedded Systems MCQs Quiz"
slug: /mcq/questions/domain/embedded/quiz
description: "A beginner-friendly embedded systems quiz on microcontrollers, interrupts, buses, DMA, and RTOS basics."
displayed_sidebar: embeddedMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# Embedded Systems MCQs Quiz



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). What is an embedded system?" options={['A computer built into a larger device', 'A kind of social network', 'A disk partition', 'A compiler warning']} answer="A computer built into a larger device" />
<Question question="2). A microcontroller usually combines:" options={['CPU, memory, and peripherals on one chip', 'Only a keyboard and screen', 'Only cloud services', 'Only a hard drive']} answer="CPU, memory, and peripherals on one chip" />
<Question question="3). Which memory is commonly used to store program code in many microcontrollers?" options={['Flash memory', 'Monitor cache', 'Browser cache', 'Clipboard memory']} answer="Flash memory" />
<Question question="4). What does ISR stand for?" options={['Interrupt Service Routine', 'Internal Source Register', 'Integrated Serial Router', 'Immediate Stack Reset']} answer="Interrupt Service Routine" />
<Question question="5). Why are interrupts useful?" options={['They let hardware signal events without constant polling', 'They replace all memory', 'They compile the program', 'They remove timers']} answer="They let hardware signal events without constant polling" />
<Question question="6). A watchdog timer is used to:" options={['Reset the system if software gets stuck', 'Increase screen brightness', 'Replace RAM', 'Compress source files']} answer="Reset the system if software gets stuck" />
<Question question="7). In embedded C, `volatile` is used when:" options={['A value can change outside normal program flow', 'A variable should always be deleted', 'You want faster optimization everywhere', 'You want to remove interrupts']} answer="A value can change outside normal program flow" />
<Question question="8). Which bus is common in automotive embedded systems?" options={['CAN', 'HDMI', 'VGA', 'DisplayPort']} answer="CAN" />
<Question question="9). I2C and SPI are mainly used for:" options={['Communication between chips/devices', 'Rendering graphics', 'Parsing source code', 'File compression']} answer="Communication between chips/devices" />
<Question question="10). PWM is commonly used for:" options={['Controlling things like motors or LEDs', 'Parsing grammar rules', 'Replacing interrupts', 'Building ASTs']} answer="Controlling things like motors or LEDs" />

<div>
    <AdBanner />
</div>

<Question question="11). DMA allows:" options={['Peripherals to transfer data without heavy CPU involvement', 'The CPU to stop existing', 'The compiler to skip parsing', 'The screen to store code']} answer="Peripherals to transfer data without heavy CPU involvement" />
<Question question="12). Memory-mapped I/O means device registers are accessed like:" options={['Normal memory addresses', 'Compressed ZIP files', 'Browser cookies', 'Only assembly comments']} answer="Normal memory addresses" />
<Question question="13). What is latency in a real-time system?" options={['Delay between an event and the response', 'The number of variables', 'Disk size', 'A kind of compiler pass']} answer="Delay between an event and the response" />
<Question question="14). Why is power optimization important in embedded systems?" options={['Many devices run on batteries or tight power budgets', 'It removes all code bugs', 'It replaces hardware design', 'It avoids interrupts']} answer="Many devices run on batteries or tight power budgets" />
<Question question="15). What is a bootloader used for?" options={['Loading the main application code at startup', 'Playing audio', 'Formatting CSS', 'Replacing timers']} answer="Loading the main application code at startup" />
<Question question="16). An RTOS helps with:" options={['Task scheduling and timing control', 'Only image editing', 'Replacing flash memory', 'Removing peripherals']} answer="Task scheduling and timing control" />
<Question question="17). Why do embedded developers care about timing?" options={['Hardware often needs predictable responses', 'Timing only matters in web apps', 'It changes source code color', 'It removes memory limits']} answer="Hardware often needs predictable responses" />
<Question question="18). Which of these is NOT usually an embedded topic?" options={['Interrupts', 'DMA', 'Context-aware CSS gradients', 'Memory-mapped I/O']} answer="Context-aware CSS gradients" />
<Question question="19). A hardware timer is commonly used to:" options={['Measure time or trigger events after delays', 'Store source code', 'Replace the CPU', 'Generate AST nodes']} answer="Measure time or trigger events after delays" />
<Question question="20). Why do beginners study buses and peripherals?" options={['To understand how the CPU talks to hardware', 'To replace algorithms', 'To avoid using code', 'To remove debugging']} answer="To understand how the CPU talks to hardware" />
<Question question="21). What does debouncing usually solve?" options={['Repeated noisy switch input changes', 'Compiler crashes', 'GPU divergence', 'Network latency']} answer="Repeated noisy switch input changes" />
<Question question="22). Which statement fits embedded programming best?" options={['Software often works close to hardware details', 'It is only about websites', 'It ignores memory completely', 'It does not use interrupts']} answer="Software often works close to hardware details" />

<div>
    <AdBanner />
</div>
