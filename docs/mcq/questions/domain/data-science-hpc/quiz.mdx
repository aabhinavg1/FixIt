---
id: hpc-quiz
title: "HPC and Performance MCQs Quiz"
slug: /mcq/questions/domain/data-science-hpc/quiz
description: "A beginner-friendly HPC quiz on parallelism, scaling, communication, accelerators, and scientific computing basics."
displayed_sidebar: hpcMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# HPC and Performance MCQs Quiz



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). HPC usually stands for:" options={['High Performance Computing', 'High Power Caching', 'Hybrid Program Compiler', 'Hardware Parsing Core']} answer="High Performance Computing" />
<Question question="2). HPC is mainly about:" options={['Solving large compute-heavy problems efficiently', 'Writing only web pages', 'Replacing operating systems', 'Drawing logos']} answer="Solving large compute-heavy problems efficiently" />
<Question question="3). Parallel computing means:" options={['Doing multiple pieces of work at the same time', 'Only using one CPU core', 'Deleting duplicate files', 'Avoiding all synchronization']} answer="Doing multiple pieces of work at the same time" />
<Question question="4). MPI is commonly used for:" options={['Communication between processes across nodes', 'Styling HTML', 'Replacing RAM', 'Building ASTs']} answer="Communication between processes across nodes" />
<Question question="5). OpenMP is often used for:" options={['Shared-memory parallel programming', 'Disk encryption', 'JavaScript bundling', 'Image export']} answer="Shared-memory parallel programming" />
<Question question="6). Amdahl’s Law helps explain:" options={['Why limited serial work can cap parallel speedup', 'How to color graphs', 'How to create tokens', 'How to build a parser']} answer="Why limited serial work can cap parallel speedup" />
<Question question="7). What is strong scaling?" options={['Making a fixed-size problem run faster with more resources', 'Increasing the problem size only', 'Using a stronger password', 'Turning off communication']} answer="Making a fixed-size problem run faster with more resources" />
<Question question="8). What is weak scaling?" options={['Growing the problem as resources grow', 'Keeping every node idle', 'Removing all data', 'Disabling scheduling']} answer="Growing the problem as resources grow" />
<Question question="9). Why does communication overhead matter in HPC?" options={['Too much communication can reduce speedup', 'It always improves performance', 'It replaces computation', 'It only affects fonts']} answer="Too much communication can reduce speedup" />
<Question question="10). Load balancing means:" options={['Spreading work fairly across resources', 'Only using one core', 'Deleting extra tasks', 'Formatting code']} answer="Spreading work fairly across resources" />

<div>
    <AdBanner />
</div>

<Question question="11). Which file format is popular for analytics and columnar storage?" options={['Parquet', 'BMP', 'TXT only', 'EXE']} answer="Parquet" />
<Question question="12). Dask is often used for:" options={['Distributed data processing in Python', 'Replacing GPUs', 'Rendering CSS', 'Compiling C++']} answer="Distributed data processing in Python" />
<Question question="13). CUDA is associated with:" options={['GPU acceleration', 'Disk scheduling', 'Network DNS', 'Markdown parsing']} answer="GPU acceleration" />
<Question question="14). BLAS and LAPACK are known for:" options={['Linear algebra operations', 'Web routing', 'Process IDs', 'Font management']} answer="Linear algebra operations" />
<Question question="15). What is latency hiding?" options={['Overlapping communication or waiting time with useful work', 'Deleting delays from code comments', 'Removing all network traffic', 'Lowering CPU clock speed']} answer="Overlapping communication or waiting time with useful work" />
<Question question="16). Backfilling in job scheduling tries to:" options={['Use spare cluster time efficiently', 'Erase old jobs', 'Replace parallelism', 'Change source code syntax']} answer="Use spare cluster time efficiently" />
<Question question="17). Why do scientific workloads often use accelerators?" options={['To speed up suitable numeric computation', 'To avoid all memory use', 'To eliminate algorithms', 'To replace filesystems']} answer="To speed up suitable numeric computation" />
<Question question="18). PCA is commonly used for:" options={['Dimensionality reduction', 'Register allocation', 'Tokenization', 'Thread cancellation']} answer="Dimensionality reduction" />
<Question question="19). In distributed systems, shuffling data usually means:" options={['Moving data between workers or nodes', 'Deleting all datasets', 'Turning arrays into text', 'Writing CSS rules']} answer="Moving data between workers or nodes" />
<Question question="20). Which issue often hurts scaling?" options={['Too much serial work or communication', 'Too many comments', 'Good locality', 'Balanced work']} answer="Too much serial work or communication" />
<Question question="21). Why do beginners learn HPC concepts?" options={['To understand how large workloads scale across resources', 'To avoid using any code', 'To replace OS design', 'To remove matrices']} answer="To understand how large workloads scale across resources" />
<Question question="22). Which topic best fits this section?" options={['MPI and scaling', 'HTML semantics', 'Browser extensions', 'Typography only']} answer="MPI and scaling" />

<div>
    <AdBanner />
</div>
