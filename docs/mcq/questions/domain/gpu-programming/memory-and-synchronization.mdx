---
id: gpu-programming-memory-sync
title: "GPU Memory and Synchronization MCQs"
slug: /mcq/questions/domain/gpu-programming/memory-and-synchronization
description: "MCQs on GPU memory hierarchy, coalescing, barriers, and data movement."
displayed_sidebar: gpuMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# GPU Memory and Synchronization MCQs



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). Which memory type is usually fastest and closest to the compute cores on a GPU?" options={['Global memory', 'Local/shared memory', 'Host memory', 'Disk cache']} answer="Local/shared memory" />
<Question question="2). What is the main drawback of global memory compared with shared/local memory?" options={['It cannot store arrays', 'It has higher latency', 'It is visible to only one thread', 'It is read-only']} answer="It has higher latency" />
<Question question="3). What does memory coalescing improve in GPU programs?" options={['Branch prediction', 'Global memory access efficiency', 'Kernel compilation time', 'Register naming']} answer="Global memory access efficiency" />
<Question question="4). A barrier inside a work-group is mainly used to:" options={['Start a new kernel', 'Synchronize participating work-items before proceeding', 'Transfer data back to the host', 'Increase register count']} answer="Synchronize participating work-items before proceeding" />
<Question question="5). If each thread reads neighboring elements from memory, the access pattern is usually:" options={['More cache-friendly and easier to coalesce', 'Guaranteed to deadlock', 'Always slower than random access', 'Invalid on GPUs']} answer="More cache-friendly and easier to coalesce" />
<Question question="6). What is the main reason to minimize host-device transfers?" options={['They can dominate total runtime', 'They increase compiler warnings', 'They reduce register pressure', 'They disable vectorization']} answer="They can dominate total runtime" />
<Question question="7). Why is local/shared memory often used in tiled GPU algorithms?" options={['It stores the executable binary', 'It allows data reuse at lower latency than global memory', 'It is larger than global memory', 'It is managed only by the CPU']} answer="It allows data reuse at lower latency than global memory" />

<div>
    <AdBanner />
</div>
