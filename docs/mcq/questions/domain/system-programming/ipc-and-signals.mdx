---
id: system-programming-ipc-signals
title: "IPC and Signals MCQs"
slug: /mcq/questions/domain/system-programming/ipc-and-signals
description: "System programming MCQs on pipes, message queues, signals, deadlocks, and synchronization."
displayed_sidebar: systemProgrammingMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# IPC and Signals MCQs



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). Which system call is used to create a pipe in Unix?" options={['pipe()', 'mkfifo()', 'dup()', 'socket()']} answer="pipe()" />
<Question question="2). Which of the following synchronization mechanisms can cause deadlock if misused?" options={['Semaphores', 'Monitors', 'Spinlocks', 'All of the above']} answer="All of the above" />
<Question question="3). In Unix, what is the default signal sent by the kill command?" options={['SIGKILL', 'SIGTERM', 'SIGSTOP', 'SIGINT']} answer="SIGTERM" />
<Question question="4). Which inter-process communication (IPC) mechanism provides communication between unrelated processes?" options={['Pipes', 'Message Queues', 'Signals', 'Shared Memory']} answer="Message Queues" />
<Question question="5). What will be the output of the following C code?" code={`#include <stdio.h>\n#include <unistd.h>\n\nint main() {\n    printf("A");\n    fork();\n    printf("B");\n    return 0;\n}`} options={['A B', 'AB', 'ABAB', 'AABB']} answer="ABAB" />
<Question question="6). Which of the following is used to prevent race conditions?" options={['Semaphores', 'Mutexes', 'Locks', 'All of the above']} answer="All of the above" />

<div>
    <AdBanner />
</div>
