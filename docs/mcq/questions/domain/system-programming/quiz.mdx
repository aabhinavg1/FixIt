---
id: system-programming-quiz
title: "System Programming MCQs Quiz"
slug: /mcq/questions/domain/system-programming/quiz
description: "A beginner-friendly system programming quiz on processes, memory, scheduling, IPC, and Unix basics."
displayed_sidebar: systemProgrammingMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# System Programming MCQs Quiz



📩 Interested in deep dives like pipelines, cache, and compiler optimizations?

<div
  style={{
    width: '100%',
    maxWidth: '900px',
    margin: '1rem auto',
  }}
>
  <iframe
    src="https://docs.google.com/forms/d/e/1FAIpQLSebP1JfLFDp0ckTxOhODKPNVeI1e21rUqMJ0fbBwJoaa-i4Yw/viewform?embedded=true"
    style={{
      width: '100%',
      minHeight: '620px',
      border: '0',
      borderRadius: '12px',
      background: '#fff',
    }}
    loading="lazy"
  >
    Loading…
  </iframe>
</div>

<Question question="1). What is a process?" options={['A running program', 'A kind of file extension', 'A network cable', 'A compiler pass']} answer="A running program" />
<Question question="2). In Unix, which system call creates a new process?" options={['fork()', 'open()', 'read()', 'sleep()']} answer="fork()" />
<Question question="3). What does `exec()` do?" options={['Creates a new disk', 'Replaces the current process image with a new program', 'Deletes memory', 'Starts the CPU fan']} answer="Replaces the current process image with a new program" />
<Question question="4). A thread is usually:" options={['A smaller unit of execution inside a process', 'A hard drive partition', 'A network protocol', 'A parser rule']} answer="A smaller unit of execution inside a process" />
<Question question="5). Threads of the same process usually share:" options={['Address space', 'Different source code languages', 'Different hard drives', 'Different CPUs only']} answer="Address space" />
<Question question="6). Which call is used to wait for a child process to finish?" options={['wait()', 'mkdir()', 'printf()', 'sort()']} answer="wait()" />
<Question question="7). What is a zombie process?" options={['A running process with high CPU use', 'A finished process that has not been fully cleaned up yet', 'A crashed browser tab', 'A kernel module']} answer="A finished process that has not been fully cleaned up yet" />
<Question question="8). Which system call creates a pipe?" options={['pipe()', 'malloc()', 'chmod()', 'ping()']} answer="pipe()" />
<Question question="9). IPC stands for:" options={['Inter-Process Communication', 'Internal Program Cache', 'Integrated Process Compiler', 'In-Place Compression']} answer="Inter-Process Communication" />
<Question question="10). Message queues are used for:" options={['Communication between processes', 'Formatting source code', 'Rendering images', 'Replacing file systems']} answer="Communication between processes" />

<div>
    <AdBanner />
</div>

<Question question="11). Virtual memory gives a process:" options={['A private view of memory addresses', 'A new keyboard', 'A faster internet connection', 'A compiler warning']} answer="A private view of memory addresses" />
<Question question="12). Which memory management method can suffer from external fragmentation?" options={['Segmentation', 'Paging only', 'Caching', 'Sorting']} answer="Segmentation" />
<Question question="13). Thrashing means:" options={['Too much paging or swapping', 'Fast CPU execution', 'Successful optimization', 'Good cache locality']} answer="Too much paging or swapping" />
<Question question="14). Round Robin scheduling is known for:" options={['Giving each task a time slice', 'Using only one process forever', 'Avoiding all context switches', 'Replacing semaphores']} answer="Giving each task a time slice" />
<Question question="15). A context switch happens when:" options={['The CPU changes from one execution context to another', 'The monitor turns off', 'The compiler changes optimization flags', 'A file is renamed']} answer="The CPU changes from one execution context to another" />
<Question question="16). A race condition happens when:" options={['Program result depends on the timing of concurrent operations', 'The compiler is too slow', 'A disk is full', 'A loop runs once']} answer="Program result depends on the timing of concurrent operations" />
<Question question="17). Which tools help prevent race conditions?" options={['Locks and mutexes', 'Comments', 'Color themes', 'File names']} answer="Locks and mutexes" />
<Question question="18). What is deadlock?" options={['When tasks wait on each other forever', 'When the CPU is idle for 1 second', 'When parsing fails', 'When files are too large']} answer="When tasks wait on each other forever" />
<Question question="19). Signals in Unix are used for:" options={['Asynchronous notifications', 'Compiling source code', 'Drawing graphs', 'Allocating GPU memory']} answer="Asynchronous notifications" />
<Question question="20). What does `dup()` do?" options={['Duplicates a file descriptor', 'Copies a folder tree', 'Duplicates the CPU', 'Creates a new process']} answer="Duplicates a file descriptor" />
<Question question="21). Why do system programmers care about processes, threads, and memory?" options={['Because they shape how low-level software really runs', 'Only for web design', 'Only for documentation', 'Only for CSS']} answer="Because they shape how low-level software really runs" />
<Question question="22). Which topic fits system programming best?" options={['fork and exec', 'Gradient colors', 'React props', 'HTTP cookies only']} answer="fork and exec" />

<div>
    <AdBanner />
</div>
