---
id: system-programming-memory-scheduling
title: "Memory and Scheduling MCQs"
slug: /mcq/questions/domain/system-programming/memory-and-scheduling
description: "System programming MCQs on segmentation, virtual memory, page replacement, and scheduling."
displayed_sidebar: systemProgrammingMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# Memory and Scheduling MCQs

<Question question="1). Which scheduling algorithm is starvation-free?" options={['FCFS', 'SJF', 'Round Robin', 'Priority Scheduling']} answer="Round Robin" />
<Question question="2). In memory management, which allocation method may suffer from external fragmentation?" options={['Paging', 'Segmentation', 'Demand Paging', 'Swapping']} answer="Segmentation" />
<Question question="3). In virtual memory, which page replacement algorithm suffers from Belady’s Anomaly?" options={['FIFO', 'LRU', 'Optimal', 'Clock']} answer="FIFO" />
<Question question="4). Which of the following best describes thrashing?" options={['CPU is idle due to I/O wait', 'Excessive swapping of pages', 'Deadlock among processes', 'Cache overflow']} answer="Excessive swapping of pages" />
<Question question="5). Which scheduling algorithm gives the minimum average waiting time?" options={['FCFS', 'SJF (non-preemptive)', 'Round Robin', 'Priority Scheduling']} answer="SJF (non-preemptive)" />
<Question question="6). Which scheduling algorithm is most suitable for time-sharing systems?" options={['FCFS', 'Round Robin', 'SJF', 'Priority']} answer="Round Robin" />

<div>
    <AdBanner />
</div>
