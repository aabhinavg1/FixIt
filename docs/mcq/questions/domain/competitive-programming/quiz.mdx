---
id: competitive-programming-quiz
title: "Competitive Programming MCQs Quiz"
slug: /mcq/questions/domain/competitive-programming/quiz
description: "A beginner-friendly competitive programming quiz on complexity, STL, graphs, DP, and common contest basics."
displayed_sidebar: competitiveProgrammingMcqSidebar
---

import AdBanner from '@site/src/components/AdBanner';
import { Question } from '../../../Question';

# Competitive Programming MCQs Quiz

<Question question="1). Competitive programming usually focuses on:" options={['Solving algorithmic problems quickly and correctly', 'Designing logos only', 'Writing CSS only', 'Managing databases only']} answer="Solving algorithmic problems quickly and correctly" />
<Question question="2). Time complexity helps you understand:" options={['How fast an algorithm grows with input size', 'How many colors a graph has', 'How many files exist', 'How bright the monitor is']} answer="How fast an algorithm grows with input size" />
<Question question="3). Which complexity is usually better for large input than O(n^2)?" options={['O(log n)', 'O(n^2)', 'O(n^3)', 'O(2^n)']} answer="O(log n)" />
<Question question="4). Which STL container gives O(1) random access?" options={['vector', 'set', 'map', 'list']} answer="vector" />
<Question question="5). `lower_bound` is commonly used on:" options={['Sorted ranges', 'Random images', 'Network sockets', 'Thread stacks']} answer="Sorted ranges" />
<Question question="6). Binary search is useful when:" options={['The answer space or data has a monotonic order', 'Everything is random with no order', 'You only have strings', 'You want to avoid loops']} answer="The answer space or data has a monotonic order" />
<Question question="7). Which sorting algorithm has O(n log n) average complexity?" options={['Merge Sort', 'Bubble Sort', 'Selection Sort', 'Insertion Sort always']} answer="Merge Sort" />
<Question question="8). A heap is useful when you often need:" options={['The smallest or largest element quickly', 'To parse source code', 'To color graphs', 'To render images']} answer="The smallest or largest element quickly" />
<Question question="9). Dynamic programming is useful when problems have:" options={['Overlapping subproblems', 'Only comments', 'No state at all', 'Only one possible answer path']} answer="Overlapping subproblems" />
<Question question="10). A graph problem often uses BFS when you need:" options={['Shortest path in an unweighted graph', 'Compilation to machine code', 'Rendering HTML', 'Disk formatting']} answer="Shortest path in an unweighted graph" />

<div>
    <AdBanner />
</div>

<Question question="11). DFS is often used for:" options={['Exploring graph structure deeply', 'Sorting arrays automatically', 'Replacing priority queues', 'Allocating DMA buffers']} answer="Exploring graph structure deeply" />
<Question question="12). Bellman-Ford is known for handling:" options={['Negative edge weights', 'Only trees', 'Only strings', 'Only heaps']} answer="Negative edge weights" />
<Question question="13). Prefix sums are helpful for:" options={['Fast range-sum queries after preprocessing', 'Parsing expressions', 'Replacing BFS', 'Compressing files']} answer="Fast range-sum queries after preprocessing" />
<Question question="14). Why do contest coders use `long long`?" options={['To reduce overflow risk for larger integers', 'To improve CSS rendering', 'To replace sorting', 'To disable recursion']} answer="To reduce overflow risk for larger integers" />
<Question question="15). `unordered_map` is popular because average lookup can be:" options={['Close to O(1)', 'Always O(n^2)', 'Always slower than loops', 'Impossible to use']} answer="Close to O(1)" />
<Question question="16). Which algorithm finds strongly connected components?" options={['Kosaraju', 'Bubble Sort', 'Binary Search', 'Heapify']} answer="Kosaraju" />
<Question question="17). What does `__builtin_popcount` count?" options={['Number of set bits', 'Number of loops', 'Number of vectors', 'Number of files']} answer="Number of set bits" />
<Question question="18). Why is practice important in competitive programming?" options={['It improves speed, pattern recognition, and accuracy', 'It removes the need for algorithms', 'It replaces math', 'It removes input/output']} answer="It improves speed, pattern recognition, and accuracy" />
<Question question="19). Which topic best matches competitive programming?" options={['Algorithms and data structures', 'Only UI design', 'Only video editing', 'Only markdown styling']} answer="Algorithms and data structures" />
<Question question="20). What is memoization?" options={['Saving results of subproblems to reuse them', 'A sorting algorithm', 'A file format', 'A network protocol']} answer="Saving results of subproblems to reuse them" />
<Question question="21). Why are graphs and DP common in contests?" options={['They appear in many problem-solving patterns', 'They are only for compilers', 'They replace arrays', 'They make code shorter automatically']} answer="They appear in many problem-solving patterns" />
<Question question="22). Which skill matters most here?" options={['Reasoning about algorithms and constraints', 'Choosing fonts', 'Editing videos', 'Writing CSS animations']} answer="Reasoning about algorithms and constraints" />

<div>
    <AdBanner />
</div>
